; std contains the `println` function
let std = @import("std")

let fibo = (n: int) -> int {
    match n {
        0, 1 => n
        _ => fibo(n - 1) + fibo(n - 2)
    }
}

; TODO: get the number from the user and use `??` for fallback
; also do it async
10 |> fibo |> println
